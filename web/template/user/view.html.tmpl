{{define "content"}}
	<div class="container">
		<p>View User</p>
		<p>ID: {{.User.ID}}</p>
		<p>Name: {{.User.Name}}</p>
		<p>Email: {{.User.Email}}</p>
		<p>Role: {{.User.Role.Name}}</p>
	</div>
{{end}}
